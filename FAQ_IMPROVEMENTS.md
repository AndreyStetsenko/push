# Улучшения FAQ компонента

## Краткое описание
Оптимизирована производительность FAQ секции с папками, особенно для мобильных устройств.

## Основные улучшения

### 1. Оптимизация производительности
- Добавлен детектор производительности устройства (`performanceDetector.js`)
- Адаптивные анимации в зависимости от мощности устройства (low/medium/high)
- Использование GPU ускорения (`transform: translateZ(0)`, `backface-visibility: hidden`)
- Оптимизация памяти через `will-change: auto` на мобильных

### 2. Исправление наложения изображений на текст + читаемость
- Добавлен `z-index` для правильного порядка слоев
- Изображения смещены выше и не залезают на контент
- Ограничены размеры изображений (`max-height`)
- Использование `isolation: isolate` для stacking context
- **Градиентный оверлей** (rgba gradient) для затемнения фона под текстом
- **Text-shadow** на всех текстовых элементах для контраста
- **Backdrop-filter: blur(2px)** для размытия фона (Chrome, Safari, Edge)
- Адаптивный градиент: сильнее на мобильных устройствах

### 3. Улучшение анимаций
- Убран лишний `will-change: transform` (оптимизация памяти)
- Отключены hover эффекты во время анимации (`.is-animating`)
- Отключены hover на слабых устройствах
- Адаптивная длительность анимаций (200ms/300ms/400ms)

### 4. Оптимизация JavaScript
- Добавлены `debounce` и `throttle` для событий
- Флаг `isProcessing` предотвращает двойные клики
- Улучшена работа с `requestAnimationFrame`
- Passive event listeners для лучшей производительности

### 5. Мобильная оптимизация
- Упрощенные анимации на мобильных (300ms вместо 400ms)
- Правильная обработка overflow
- Touch-friendly взаимодействие
- Адаптивные размеры изображений

### 6. Accessibility
- Поддержка `prefers-reduced-motion`
- Отключение анимаций для пользователей с ограниченными возможностями

## Измененные файлы
- `src/js/components/faqFolders.js` - основная логика
- `src/js/utils/performanceDetector.js` - новый файл для определения производительности
- `src/scss/components/faq.scss` - стили и анимации
- `src/js/main.js` - подключение детектора производительности

## Технические детали

### CSS переменные
- `--faq-transition-duration` - динамически устанавливается JS в зависимости от производительности
- `--transition-time` - локальная переменная для каждой папки

### Классы производительности
- `data-performance="low"` - слабое устройство (2 ядра, 2GB RAM)
- `data-performance="medium"` - среднее устройство (4 ядра, 4GB RAM)
- `data-performance="high"` - мощное устройство (8+ ядер, 8GB+ RAM)
- `.slow-device` - добавляется к `<html>` для слабых устройств

### Конфигурация анимаций
```javascript
low: {
    transitionDuration: 200ms,
    enableHoverEffects: false,
    reducedMotion: true
}
medium: {
    transitionDuration: 300ms,
    enableHoverEffects: true
}
high: {
    transitionDuration: 400ms,
    enableHoverEffects: true
}
```

## Тестирование
Протестируйте на:
- Мощных десктопах (Chrome, Safari, Firefox)
- Слабых мобильных устройствах (Android)
- iPhone (Safari)
- Медленном интернете (3G)

## Компиляция
После изменений не забудьте скомпилировать JS и SCSS файлы:

```bash
# Перейти в директорию темы
cd wp-content/themes/push

# Установить зависимости (если еще не установлены)
npm install

# Скомпилировать проект
npm run build

# Или запустить watch режим для автоматической компиляции
npm run watch
```

## Результаты
✅ CSS скомпилирован: `assets/css/main.min.css` (92KB)
✅ JS скомпилирован: `assets/js/main.min.js` (91KB)
✅ Все изменения применены успешно

## Что изменено в этом коммите

### JavaScript файлы:
1. `src/js/components/faqFolders.js` - оптимизированная логика папок FAQ
2. `src/js/utils/performanceDetector.js` - новый утилитный класс для определения производительности
3. `src/js/main.js` - добавлен импорт детектора производительности

### SCSS файлы:
1. `src/scss/components/faq.scss` - улучшенные стили и анимации

### Конфигурация:
1. `gulpfile.js` - убран проблемный Babel loader
2. `.babelrc` - добавлена конфигурация Babel
3. `FAQ_IMPROVEMENTS.md` - документация об изменениях

### Преимущества:
- ⚡ Быстрее работает на слабых устройствах (200ms vs 400ms анимации)
- 🎯 Изображения не залезают на текст
- 📱 Оптимизировано для мобильных устройств
- 🚀 GPU ускорение через CSS transforms
- ♿ Поддержка accessibility (prefers-reduced-motion)

