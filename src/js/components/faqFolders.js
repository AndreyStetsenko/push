/**
 * FAQ Folders Component (Accordion)
 * Pure CSS accordion - JS только для дополнительных эффектов если нужно
 */

export function initFaqFolders() {
    const foldersContainer = document.querySelector('.faq__folders');
    if (!foldersContainer) return;

    // Accordion теперь работает на чистом CSS через checkbox
    // Оставляем функцию для возможных будущих доработок
    console.log('FAQ Accordion initialized');
}
